open Extra

let dumpfile = STO.dumpfile
let loadfile = STI.loadfile

let print = STO.print_treelist
let pprint_v1 = STO.pprint_v1
let pprint_v2 = STO.pprint_v2
let pprint = pprint_v2

let of_string = STD.string
let to_string = STL.string

let of_bool = STD.bool
let to_bool = STL.bool

let of_int = STD.int
let to_int = STL.int

let of_float = STD.float
let to_float = STL.float

let of_unit = STD.unit
let to_unit = STL.unit

let of_option = STD.option
let to_option = STL.option

let of_list = STD.list
let to_list = STL.list

let of_array = STD.array
let to_array = STL.array

let of_pair = STD.pair
let to_pair = STL.pair

let of_trio = STD.trio
let to_trio = STL.trio
