let bitv_bool = (StrDump.bitv_bool, StrLoad.bitv_bool)
let bitv_hexa = (StrDump.bitv_hexa, StrLoad.bitv_bool)
let string = (StrDump.string, StrLoad.string)
let unit = (StrDump.unit, StrLoad.unit)
let bool = (StrDump.bool, StrLoad.bool)
let int = (StrDump.int, StrLoad.int)
let float = (StrDump.float, StrLoad.float)
